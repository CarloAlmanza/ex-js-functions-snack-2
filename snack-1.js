// 1. Funzione dichiarativa
function somma(a, b) {
    return a + b;
}

// 2. Funzione anonima assegnata a una variabile
const somma = function (a, b) {
    return a + b;
};

// 3. Arrow function
const somma = (a, b) => a + b;